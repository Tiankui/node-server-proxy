var redis = require('socket.io/node_modules/redis');
var sysConfig = require('../../config.js');

exports.createClent = function(){
    return redis.createClient(sysConfig.REDIS.PORT,sysConfig.REDIS.HOST);
}
